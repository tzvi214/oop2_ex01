#include <iostream>
#include <memory>
#include "SqrMatrix.h"
#include "Add.h"

using namespace std;

int main() {
    //// יצירת מטריצות לבדיקה
    //SqrMatrix A(2), B(2);

    //cout << "Enter values for matrix A (2x2):" << endl;
    //cin >> A;

    //cout << "Enter values for matrix B (2x2):" << endl;
    //cin >> B;

    //// הצגת המטריצות שהוזנו
    //cout << "Matrix A:\n" << A << endl;
    //cout << "Matrix B:\n" << B << endl;

    //// יצירת אובייקט Add
    //shared_ptr<Operation> op1 = make_shared<Operation>("A");
    //shared_ptr<Operation> op2 = make_shared<Operation>("B");
    //shared_ptr<Add> addOp = make_shared<Add>(op1, op2);

    //// חישוב חיבור המטריצות והדפסת התוצאה
    ////SqrMatrix result = addOp->calc(A, B);

    //cout << "A + B name\n";
    //addOp->printName() ;

    return 0;
    
}
